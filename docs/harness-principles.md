# Harness 原则

- 项目事实存放在目标项目 Harness。
- 模板通用，不写死业务事实。
- 未知信息标记 `TODO`。
- 每个功能必须有验收标准与验证方式。
- 使用 sub-agent 时，必须先定义任务边界与可改动范围。
- 每个 sub-agent 任务必须有明确输入、预期输出、验收标准。
- 多 sub-agent 协作禁止并发改同一文件，最终由主 agent 统一集成与记录。
